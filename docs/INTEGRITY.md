# نظام النزاهة (كشف الغش والتلاعب)

> التطبيق: `backend/integrity/` · اللوحة: `/ops/integrity/` · المحاكاة: `python manage.py simulate_city`

## الفكرة بسطرين
النظام **بيلاحظ** (إشارات) و**بيقيّد مؤقتًا** إذا تراكمت الإشارات، بس **ما بيحظر حدا لحاله أبدًا**. الحكم النهائي دائمًا لموظف.

## كيف بتنحسب النقاط
- كل نمط غش إلو **وزن** (جدول تحت).
- الوزن **بيتلاشى**: بعد 14 يوم بيصير نصه، بعد شهر ربعه — اللي بيغلط مرة بيرجع نظيف لحاله.
- النقاط = مجموع الأوزان بعد التلاشي (من 0 لـ100).

| النقاط | المستوى | شو بيصير |
|---|---|---|
| أقل من 20 | سليم | لا شي |
| 20–39 | تحت المراقبة | ملاحظة تلقائية على الحساب |
| 40–69 | بحاجة مراجعة | **قضية** بتنفتح بلوحة الأدمن |
| 70+ | مقيّد | **تقييد مؤقت** لحتى يراجعه موظف |

**التقييد (قابل للتراجع):**
- **الزبون:** بدون خصومات وأرصدة (الرصيد بيضل محفوظ)، وبدون «اختر سيارتك». بيقدر يطلب بالسوم عادي.
- **السائق:** ما بيوصله «الأقرب» تلقائيًا، وما بياخد مكافآت الحوافز. بيقدر يقدّم عروض بالسوم عادي.

**إشارة وحدة ما بتكفي للتقييد أبدًا:** أقوى إشارة وزنها 35 (تحت المراقبة). لأنو عيلة بتتشارك موبايل، أو سائق فقد الـGPS بنفق، مو محتالين.

## الأنماط اللي بيكشفها
| النمط | على مين | الوزن | كيف |
|---|---|---|---|
| قفزة موقع مستحيلة | سائق | 6 | سرعة > 200 كم/س بين نقطتين (مرة كل ربع ساعة كحد أقصى) |
| موقع مزيّف (تطبيق Mock) | سائق | 15 | أندرويد بيعلن إنو الموقع محاكى — **والموقع بيترفض** |
| تطويل الطريق | سائق | 8 | المسافة الفعلية أكتر من 1.5 ضعف المتوقعة وبفرق أكتر من 2 كم |
| الزبون قال إنو السائق طلب منه يلغي | سائق | 15 | رمز سبب الإلغاء `driver_asked` |
| إلغاء متكرر بعد الوصول لنفس الثنائي | الاتنين | 20 (الزبون 10) | مرتين بأسبوعين = غالبًا بيكملوا برّا التطبيق |
| نسبة إلغاء عالية بعد القبول | سائق | 20 | أكتر من 30٪ من 8 رحلات مقبولة فأكتر بالأسبوع |
| زبون بيخلي السائق ينطر وبيلغي | زبون | 20 | مرتين بأسبوع بعد انتظار 5 دقائق فأكتر |
| سائق وزبون على نفس الجهاز وبيناتهن رحلات | الاتنين | 35 | من `device_id` بتسجيل الدخول |
| رحلات قصيرة متكررة لنفس الثنائي | الاتنين | 20 (الزبون 10) | 4 رحلات فأكتر بالأسبوع بمعدل أقل من 1.5 كم |
| رحلات قصيرة جدًا بكثافة | سائق | 15 | 8 رحلات فأكتر باليوم أقل من 700 م أو 3 دقائق |
| شكوى أجرة على رحلة سوم (سعر طُعم) | سائق | 12 | الزبون اشتكى من الأجرة على سعر متفق عليه بعرض |
| عدة حسابات على جهاز واحد | زبون | 20 | 3 حسابات فأكتر على نفس الجهاز خلال شهر |
| خصم أول رحلة من عدة حسابات | زبون | 30 | حسابين فأكتر على نفس الجهاز أخدوا الخصم |
| دعوة صديق من نفس الجهاز | زبون | 35 | الداعي والمدعو نفس الموبايل |
| بلاغ يدوي | أي حدا | 25 | موظف بيضيفه |

## شو بيعمل المشرف
1. بيفتح `/ops/integrity/`: القضايا المفتوحة، أعلى الحسابات خطرًا، والأنماط، وتحت كل نمط شو لازم يفحص.
2. بيفتح القضية، بيشوف الإشارات بأدلتها والملاحظات.
3. بيختار:
   - **«أكّد الغش»:** الحساب بيضل مقيّد بقرار إداري، حتى لو النقاط تلاشت.
   - **«إنذار كاذب»:** الإشارات بتنشطب، وبيتعلّم الحساب «سليم» 30 يوم.
   - أو من «ملفات الخطر»: **سليم 30 يوم**، **قيّد يدويًا**، **رجّعه للنقاط**، **أضف ملاحظة**.

## الإعدادات (متغيرات البيئة)
| المتغير | الافتراضي | المعنى |
|---|---|---|
| `INTEGRITY_HALF_LIFE_DAYS` | 14 | عمر النصف لوزن الإشارة |
| `INTEGRITY_WATCH_AT` / `REVIEW_AT` / `RESTRICT_AT` | 20 / 40 / 70 | حدود المستويات |
| `INTEGRITY_AUTO_RESTRICT` | True | False = النظام بيلاحظ وبيفتح قضايا بس، والتقييد قرار موظف |
| `INTEGRITY_REJECT_MOCK_LOCATIONS` | True | رفض المواقع المحاكاة |

## ربط تطبيقَي الفلاتر ✅ (منفّذ)
1. **الموقع (تطبيق السائق):** كل `location.update` بالـWebSocket بيحمل `mocked` (من `position.isMocked`) ومعه السرعة (م/ث) والاتجاه والدقة — `apps/driver/lib/features/presence/location_fix.dart`.
   لمّا الخادم يرفض الموقع بحدث `location.rejected` بالسبب `mock_location`، السائق بيضل «متصل» بس **خارج المطابقة**، وبيطلعله: «هاتفك يرسل موقعًا مزيّفًا… أطفئ تطبيق تزييف الموقع». أول ما يطفيه ويوصل موقع حقيقي بيرجع للمطابقة لحاله.
2. **إلغاء الزبون (تطبيق الزبون):** زر «ألغِ الرحلة» صار بيفتح قائمة أسباب — اختيار السبب هو التأكيد. الرمز بينبعت كـ`reason_code` على المسارين:
   `POST /api/v1/rides/{id}/cancel/` (اللي بيستعمله التطبيق) و`POST /api/v1/customer/rides/{id}/cancel-trip/`.
   الرموز: `driver_late` السائق تأخر · `driver_not_moving` السائق ما عم يتحرك · `driver_asked` **السائق طلب مني ألغي** · `wrong_pickup` مكان الالتقاط غلط · `found_other` لقيت وسيلة تانية · `changed_mind` غيّرت رأيي · `other` سبب تاني.
   اختبار عقد بيتأكد إنو رموز التطبيق = رموز الخادم بالاتجاهين.
3. **قنوات الإشعارات بأندرويد:** `soum_urgent` و`soum_general` بتنشأ بـ`MainActivity.kt` بالتطبيقين.

## المحاكاة
```bash
python manage.py simulate_city --days 3 --drivers 40 --customers 600 --report sim.md
python manage.py simulate_city --ramadan --days 2 --report ramadan.md
```
- يوم كامل بمنحنيات طلب حقيقية (ذروة 7–9، 13–15، 17–20، ليلة الخميس أتقل، صباح الجمعة خفيف، ونمط رمضان).
- بتشتغل على **الخدمات الحقيقية**: طلب ← عروض سوم ← اختيار ← وصول بنصف القطر الحقيقي ← بدء ← نقاط مسار ← إنهاء ← دفعة، إلغاء بالسياسة الحقيقية، شكاوى.
- شخصيات غشاشة معروفة مسبقًا + "ضوضاء" لأشخاص نظاميين (موبايل عيلة، فقدان GPS مرة، مشوار يومي، زحمة) — منشان نقيس مين انكشف، مين فلت، **ومين انظلم**.
- التقرير فيه: الذروة ساعة بساعة (طلبات، تطابق، طلبات ضايعة، سائقين أونلاين، انتظار)، ودقة الكشف، والملاحظات والتعيينات على كل حساب غشاش.
- ما بتشتغل على قاعدة الإنتاج. والبيانات بتضل (دفتر المدفوعات ما بيقبل حذف بتصميمه) — شغّلها على قاعدة تطوير.
